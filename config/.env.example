# Example .env (DO NOT COMMIT real keys)
# Use an IAM user (not root) or prefer AWS_PROFILE / IAM roles instead of long-term keys.

# Option A: Use AWS CLI profile. Configure with `aws configure --profile myprofile` and set AWS_PROFILE=myprofile
# AWS_PROFILE=myprofile

# Option B: Provide temporary creds (e.g., from STS assume-role) or environment variables for local dev
# AWS_ACCESS_KEY_ID=YOUR_IAM_USER_ACCESS_KEY_ID
# AWS_SECRET_ACCESS_KEY=YOUR_IAM_USER_SECRET_ACCESS_KEY
# Optional: set region here or rely on default
# AWS_REGION=ap-south-1

# Mongo / other env vars used by project
# MONGO_URL=mongodb://localhost:27017/gitOrbit
# PORT=3000
